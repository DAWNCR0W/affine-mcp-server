# CLAUDE.md

## Environment

- **Platform**: Windows (MINGW64 / Git Bash)
- **Absolute path to this repo**: `//nas/git/AI/affine-mcp-server`
  - Use `//nas/git/AI/...` (double forward slash) — not `/nas/git/AI/...`
  - Git may report "dubious ownership" on this network path; this is expected and non-blocking for file operations
- **Shell**: Use Unix shell syntax (forward slashes, `/dev/null`, etc.) — not Windows cmd/PowerShell syntax
